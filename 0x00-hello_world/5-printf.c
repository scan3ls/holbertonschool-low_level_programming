#include <stdio.h>
/**
 * main - Display a message with printf()
 *
 * Return: return a value of 0
 */
int main(void)
{
printf("%s", "with proper grammar, but the outcome is a piece of art,\n\0");
return (0);
}
